function soma(a, b) {
    console.log(`${a} + ${b} = ${a+b}`)
}

soma(10, 5)

function subtracao(a, b) {
    console.log(`${a} - ${b} = ${a-b}`)
}

subtracao(10, 5)

function multiplicacao(a, b) {
    console.log(`${a} * ${b} = ${a*b}`)
}

multiplicacao(10, 5)